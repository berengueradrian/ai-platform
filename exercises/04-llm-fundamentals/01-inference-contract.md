# Exercise 01 — Provider-neutral inference contract

Design an inference interface that supports streaming, cancellation, timeouts, structured
outputs, tool calls, usage accounting, and provider errors. Start with a fake provider and
tests. Compare retryable and non-retryable failures before connecting a real model.
