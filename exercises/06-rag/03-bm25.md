# Exercise 03 — Lexical retrieval baseline

Implement or integrate a BM25 baseline over the same corpus and query set. Investigate
exact identifiers, rare terms, error codes, morphology, and paraphrases. Compare failure
categories without changing the corpus or labels.
