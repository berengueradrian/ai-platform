# Exercise 09 — Bad-case forensics

Build a failure corpus and classify misses: parsing, chunking, query mismatch, index,
ranking, context budget, generation, or evaluator error. Turn at least three findings
into regression tests.
