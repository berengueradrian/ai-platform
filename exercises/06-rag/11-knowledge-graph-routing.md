# Exercise 11 — Knowledge graph augmentation and routing

Extend the RAG system only for query families that require entities, relationships, or
multi-hop constraints. Define a router between lexical, dense, and graph retrieval; measure
routing accuracy, provenance, latency, and fallback behavior. Include a non-graph baseline
and document when graph complexity is not justified.
