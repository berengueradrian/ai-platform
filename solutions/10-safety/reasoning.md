# Reasoning guide — safety

Prompt text is not an authorization boundary. Separate untrusted content, model instructions,
policy decisions, and tool execution. Defense in depth means least privilege, explicit confirmation,
output validation, isolation, monitoring, and adversarial regression tests; document residual risk.
