# Reasoning guide — capstone

The platform should expose stable primitives—model access, retrieval, evaluation, policy, telemetry,
and state—while keeping application workflows replaceable. Integrate only components with evidence,
define SLOs and rollback paths, and defend every cross-cutting abstraction against unnecessary coupling.
