# Reasoning guide — deep learning

Diagnose training in layers: data/labels, tensor contracts, optimization, capacity, regularization,
and measurement. Learning curves and gradient statistics should narrow the hypothesis before a
hyperparameter sweep. Every intervention should change one meaningful variable and retain a baseline.
