# Evaluation platform

Build this after Phase 5 exercises. It should version datasets, run deterministic metrics
and calibrated judges, report slices, and act as a regression gate. Begin with a design and
contract; do not add a UI until the evaluation semantics are trustworthy.
