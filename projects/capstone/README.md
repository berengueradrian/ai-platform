# Integrated AI platform capstone

No implementation yet. The capstone will compose proven components from the preceding projects:
gateway, routing, cost, cache, RAG, graph, tools, state, evaluation, observability, flags, and safety.

Required first artifact: an architecture proposal with non-goals, boundaries, SLOs, threat model,
and an integration test plan.
