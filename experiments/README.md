# Experiments

Store one hypothesis per report using `templates/EXPERIMENT.md`. Keep raw outputs out of
Git when large; record stable paths, dataset versions, seeds, and summary tables. Use
`templates/BENCHMARK.md` for repeated workload comparisons.
